# 1️⃣ Start from a lightweight official Python base image
FROM python:3.10-slim

# 2️⃣ Set the working directory inside the container
WORKDIR /app

# 3️⃣ Copy the requirements file first and install dependencies
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 4️⃣ Copy your application code
COPY app.py .

# 5️⃣ Expose port 8000 so Docker knows which port the app uses
EXPOSE 8000

# 6️⃣ Run FastAPI using Uvicorn
CMD ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "8000"]

